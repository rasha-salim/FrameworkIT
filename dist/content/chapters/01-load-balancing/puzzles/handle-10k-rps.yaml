id: lb-001-handle-10k-rps
title: Handle 10,000 Requests Per Second
chapter: 01-load-balancing
difficulty: 2

briefing:
  npc: sarah-the-sre
  text: >
    Our single web server is crashing under load. 10,000 req/s peak,
    but one server handles 2,000 max. Design a system that handles it all
    with p99 latency under 200ms.

fixedComponents:
  - id: client-pool
    type: client-pool
    position:
      x: 50
      y: 300
    config:
      requestsPerSecond: 10000
      distribution: uniform
  - id: existing-server
    type: web-server
    position:
      x: 600
      y: 300
    config:
      maxRPS: 2000
      baseLatencyMs: 20

availableComponents:
  - type: load-balancer
    maxCount: 2
    costPerUnit: 150
  - type: web-server
    maxCount: 8
    costPerUnit: 100

objectives:
  bronze:
    - metric: throughput
      operator: ">="
      value: 10000
    - metric: error_rate
      operator: "<="
      value: 5
  silver:
    - metric: p99_latency
      operator: "<="
      value: 200
  gold:
    - metric: total_cost
      operator: "<="
      value: 800

simulation:
  durationSeconds: 60
  tickRateMs: 100

debrief:
  - id: what-you-did
    question: "Explain in your own words what you built and why it handles the traffic that a single server couldn't."
    hint: "Think about how the load balancer distributes requests and what happens when you add more servers."
  - id: tradeoffs
    question: "What trade-offs did you consider? For example, why not just use one very powerful server instead?"
    hint: "Consider cost, single points of failure, and the limits of vertical vs horizontal scaling."
